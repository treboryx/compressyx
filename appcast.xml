<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>Compressyx</title>
        <description>Compressyx update feed</description>
        <language>en</language>
        <item>
            <title>Version 1.0.2</title>
            <sparkle:version>1.0.2</sparkle:version>
            <sparkle:shortVersionString>1.0.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <pubDate>Sun, 23 Aug 2026 16:25:02 +0000</pubDate>
            <description><![CDATA[<style>
:root { color-scheme: light dark; }
body {
  font: -apple-system-body, -apple-system, system-ui, sans-serif;
  margin: 0; padding: 12px 14px;
  color: #1d1d1f; background: transparent;
}
h3 { font-size: 1.02em; margin: 1.1em 0 0.45em; letter-spacing: -0.01em; }
h3:first-child { margin-top: 0; }
ul { margin: 0 0 0.5em; padding-left: 1.25em; }
li { margin-bottom: 0.35em; }
code {
  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
  font-size: 0.9em; background: rgba(127,127,127,0.16);
  padding: 0.1em 0.32em; border-radius: 4px;
}
@media (prefers-color-scheme: dark) { body { color: #f5f5f7; } }
</style><h3>Added</h3><ul><li>WebP output, via <code>cwebp</code>. Works from every supported input, including HEIC. On a 600×600 sample it produced the smallest file of any format — 12.5 KB, against 15.4 KB for HEIC and 43 KB for JPEG.</li><li><strong>Install All Missing</strong> in Settings → Dependencies, which installs FFmpeg, pngquant and cwebp in a single Homebrew invocation.</li><li>Settings now persist across launches. Codec, quality preset, output formats, output folder, delete-original and concurrency are all remembered.</li></ul><h3>Fixed</h3><ul><li><strong>The image output format picker did nothing.</strong> It changed the output file&#x27;s extension without changing the encoder, so choosing PNG → JPEG wrote PNG bytes into a file named <code>.jpg</code>. The encoder is now selected from the chosen format rather than the input file&#x27;s extension.</li><li><strong>WebP output silently produced nothing.</strong> The old code encoded JPEG, wrote it to a sibling <code>.jpg</code> path, then reported success for a <code>.webp</code> file that was never created — surfacing in the UI as a 0-byte compression.</li><li>TIFF and BMP with &quot;same as input&quot; selected produced a file named <code>.tiff</code> containing JPEG data.</li><li>PNG output from a non-PNG source failed, because pngquant only reads PNG. Such sources are now transcoded first.</li><li>When pngquant declined to compress, the fallback copied the <em>original</em> input to the output path, which could write JPEG bytes to a <code>.png</code> name.</li><li>The Dependencies tab&#x27;s Install buttons did nothing when Homebrew was absent. It now says Homebrew is required and links to brew.sh.</li></ul><h3>Changed</h3><ul><li>Dependency status refreshes when the Settings tab appears, so tools installed in Terminal are picked up without relaunching.</li><li>A failed install now names the exact <code>brew install</code> command to run manually.</li><li><code>compress()</code> verifies the output file exists before returning, so an encoder that writes nothing fails loudly instead of reporting a 0-byte success.</li></ul>]]></description>
            <enclosure
                url="https://github.com/treboryx/compressyx/releases/download/v1.0.2/Compressyx-1.0.2.dmg"
                length="2879624"
                type="application/octet-stream"
                sparkle:edSignature="/QY24jDIvYb/unGgtYNAJ3vZNmhK9TUxY27H0x7JQE1bnAmghwR8/tUv5zgmMbRse68UYcRzIRvgCzFN9GveCA==" />
        </item>
    </channel>
</rss>
