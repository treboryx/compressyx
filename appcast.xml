<?xml version="1.0" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>Compressyx</title>
        <description>Compressyx update feed</description>
        <language>en</language>
        <item>
            <title>Version 1.1.0</title>
            <sparkle:version>1.1.0</sparkle:version>
            <sparkle:shortVersionString>1.1.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
            <pubDate>Sun, 23 Aug 2026 17:10:32 +0000</pubDate>
            <description><![CDATA[<style>
:root { color-scheme: light dark; }
body {
  font: -apple-system-body, -apple-system, system-ui, sans-serif;
  margin: 0; padding: 12px 14px;
  color: #1d1d1f; background: transparent;
}
h3 { font-size: 1.02em; margin: 1.1em 0 0.45em; letter-spacing: -0.01em; }
h3:first-child { margin-top: 0; }
ul { margin: 0 0 0.5em; padding-left: 1.25em; }
li { margin-bottom: 0.35em; }
code {
  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
  font-size: 0.9em; background: rgba(127,127,127,0.16);
  padding: 0.1em 0.32em; border-radius: 4px;
}
@media (prefers-color-scheme: dark) { body { color: #f5f5f7; } }
</style><h3>Added</h3><ul><li>Release notes in the update dialog. The appcast now carries the matching CHANGELOG section, so Sparkle shows what changed instead of a bare version number. Takes effect from the next release.</li><li><strong>Folders can be dropped on the app</strong>, and are expanded recursively. Hidden files are skipped and bundles such as <code>.photoslibrary</code> are not descended into. The README has claimed this since 1.0.0; it now works.</li><li><strong>Metadata control</strong> in Settings — <em>Preserve</em>, <em>Remove location</em>, or <em>Remove all</em>. Previous versions silently stripped every tag from every image, including the capture date, which is how Photos and the Finder sort images. The new default is <em>Preserve</em>.</li><li><strong>Advanced video options</strong>, behind a collapsed &quot;Advanced&quot; section in the sidebar: downscale to 4K/1440p/1080p/720p/480p, re-encode audio to AAC or remove it, and cap the frame rate. Downscaling never upscales a smaller source. Defaults leave every existing behaviour unchanged.</li></ul><h3>Fixed</h3><ul><li><strong>WebM output failed for every video with AAC audio.</strong> The audio track was stream-copied into a container that accepts only Vorbis and Opus, so ffmpeg aborted and no file was produced. WebM now encodes audio as Opus.</li><li>The main window&#x27;s dependency check ignored <code>cwebp</code>, so a missing WebP encoder went unreported until a job failed.</li><li>Dropping a folder and one of its own files in the same gesture queued that file twice.</li></ul><h3>Changed</h3><ul><li>Image metadata defaults changed from &quot;strip everything&quot; to &quot;preserve everything&quot;. If you relied on the old behaviour to remove GPS data, choose <em>Remove location</em> or <em>Remove all</em> in Settings.</li></ul>]]></description>
            <enclosure
                url="https://github.com/treboryx/compressyx/releases/download/v1.1.0/Compressyx-1.1.0.dmg"
                length="2924870"
                type="application/octet-stream"
                sparkle:edSignature="TeCCXrQU2a0Or00EeL5QNRpH3Gjy1ykFP0om7fAOIAtB3FdK1l9xCMOTvHbFtQTRBPNITwEELBB5MIhWGQIHAw==" />
        </item>
    </channel>
</rss>
